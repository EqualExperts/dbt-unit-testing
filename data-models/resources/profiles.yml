default:
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: [YOUR GCP PROJECT]
      dataset: covid19 # You can also use "schema" here
      threads: 1
      timeout_seconds: 300
      location: US # Optional, one of US or EU
      priority: interactive
      retries: 1

  target: dev
