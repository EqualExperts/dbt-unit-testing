integration_tests:
  target: bigquery
  outputs:
    bigquery:
      type: bigquery
      method: service-account
      project: "{{ env_var('BIGQUERY_TEST_PROJECT') }}"
      dataset: "{{ env_var('BIGQUERY_TEST_DATASET') }}"
      threads: 1

